# ──────────────────────────────────────────────────────────────────────────────
# Skeleton Shorts — Video Automation Pipeline
# requirements.txt
# ──────────────────────────────────────────────────────────────────────────────

# LLM (script/voiceover) + Image Generation — both use Google Gemini / GenAI
google-genai>=0.8.0

# Audio — ElevenLabs TTS with timestamps
elevenlabs>=1.2.0

# Video Rendering
moviepy==1.0.3                # pinned: v2.x has breaking API changes
Pillow>=10.0.0                # PIL — caption drawing

# HTTP / Async HTTP (Kling polling)
requests>=2.31.0
aiohttp>=3.9.0

# Env / Utilities
python-dotenv>=1.0.0
tqdm>=4.66.0                  # progress bars for polling loops

# NumPy   (moviepy dependency + PIL → numpy array conversion)
numpy>=1.24.0

# Typing helpers
typing-extensions>=4.9.0
